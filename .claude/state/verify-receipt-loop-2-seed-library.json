{
  "task_id": "loop-2-seed-library",
  "branch": "lane-backend/loop-2-seed-library",
  "verified_at": "2026-07-30T00:45:00-04:00",
  "checks": {
    "backend_tests": "pytest tests/unit/: 2010 passed (1995 on main + 15, +1 more after /simplify = 16 in test_seed_library.py). Content gate loads the REAL starter_library/soap.json through the service's own create-time validator (schema + caps + section contract: exactly subjective/objective/assessment/plan, all required, visual machinery on objective only, 'never infer' copy canary, no system_prompt). Upsert contract: created (is_shared=True, resolved admin owner) / unchanged (canonical content compare) / updated-in-place (original owner kept) / cross-owner duplicate-key -> loud abort / service rejection -> SeedError / schema-invalid payload rejected by the REAL service gate. Owner resolution: unknown email and non-admin role rejected, ADMIN accepted (users_repo.get_by_email + UserRole enum).",
    "acceptance_criteria": "AC-1 soap.json parses + passes service caps (real file); AC-2 section contract asserted; AC-3 idempotency (second run writes nothing, create/update assert_not_awaited); AC-4 drift -> exactly one in-place service update + unknown owner email aborts before writes; AC-5 is_shared=True on create + loud failure on validation errors; AC-6 full unit suite 2010 green, ruff clean (app/ scripts/ tests/).",
    "lint": "ruff check app/ scripts/ tests/ -> All checks passed.",
    "stack_boots": "n/a here - no runtime code changed (new script + data file + tests only); the API surface is untouched.",
    "ios_builds": "n/a - backend-only; iOS receives the template through existing shared-row resolution (mobile constraint honoured: zero client key lists).",
    "simplify": "4 agents (reuse/simplify/efficiency/altitude). Applied: users_repo.get_by_email + UserRole enum in resolve_owner (reuse - was the 3rd copy of the email query); shared set fetched once as {key: rows} + >1-rows-per-key loud abort (altitude: DB uniqueness is (owner_id,key) so cross-owner duplicates could silently clobber a hand-authored row); unmanaged-shared-rows report (altitude: half-reproducible Library must say so); single validation gate per path + content-only drift compare (simplification); tests restyled to AsyncMock house idiom with flat script loader; soap.json drops default-value measurement flags. Conscious keeps: lazy imports (ruff E402 active for scripts/ in CI - verified), SeedError (context + exit codes), guidance descriptive-mode phrasing in triplicate (deliberate per-section steering; 'never infer' test is the tripwire). Deferred with named triggers: service-level upsert_shared (second consumer of upsert-by-key); UNIQUE(key) WHERE is_shared index + admin-UI shared-key 409 (second active curator or multi-org); scripts/_bootstrap extraction (pre-existing 4th shim copy); retro-fitting the four admin-authored Library rows into starter_library/ (the unmanaged report names them meanwhile). Flag-flip note from altitude: SOAP is the fleet's first template carrying detail_level=standard - when template_engine/detail-level behaviour changes, SOAP's output character shifts while built-ins' does not; add to the flip receipt checklist.",
    "operator_step_post_merge": "Run against dev: python scripts/seed_library.py --owner-email <ADMIN email> (or admin-UI the same JSON). Expect 'created: soap_universal' + an 'unmanaged' line naming the four hand-authored rows; rerun -> 'unchanged'. Then verify a clinician account sees 'SOAP - Universal' under Templates -> Library."
  }
}
