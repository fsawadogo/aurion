{
  "task_id": "te-4h-visit-types-one-surface",
  "branch": "lane-web/te-4h-visit-types-one-surface",
  "verified_at": "2026-07-29T22:30:00-04:00",
  "checks": {
    "code_review": "pr-review-toolkit code-reviewer (PR #700): verdict REQUEST CHANGES — 2 Important, both FIXED (c9ee051), suite now 431. (1) setDefaultTemplate could insert a 31st row past the backend 30-cap -> whole PUT 422s (blocks unrelated Profile edits); fixed: select disabled when list full + no default (replacement stays editable), handler guard, 2 boundary tests. (2) Profile passed OWNED-only customs on a stale pre-#619 justification -> a healthy shared-template default read 'Custom template (unavailable)' in Profile's new header; fixed: both doors pass the owned+shared list (get_owned_or_shared is the real backend binding), stale contract comments corrected. Minor fixed: FR default.* terminology/apostrophes/self-repeating summary. Accepted-as-is (traced clean by reviewer): re-pick moves default row to end -> conservative dirty only (resolution scans the flag, audit diffs by id, React keys by id); hidden default label in dup-check (backend validates full list). Reviewer verified: templateOptions equivalence incl. the != null unification being strictly safer; dirty.ts covers every editor-writable field; no new PHI/aria leak; no new dependency on the tab spec's 3-key stub; i18n keys parity + zero refs to the 7 removed keys.",
    "web_tests": "vitest run (web/): 51 files / 429 passed (427 post-implement + 2 dirty-check). VisitTypeContexts.spec 30/30 incl. 8 new TE-4h tests: custom pick upserts is_default row preserving named contexts; '' drops the row (map key removed); re-pick preserves id/label/description; legacy built-in default -> visible re-pick option then clearable; stale-ref default -> customUnavailable placeholder (NEW guard, closes the clinician-default half of chip task_8882a9c7); is_default never renders as a context row + badge counts named only; collapsed summary names the default; deleting a named context leaves the default intact. VisitTypesTab.spec: clinician = accordion-only surface (no flat selects), draft/dirty/save wiring via editor stub, admin tests untouched green. profile-dirty-check: is_default-only flip dirties Profile's Save; missing vs explicit false not spuriously dirty. i18n-bootstrap EN/FR parity green (default.* added, 7 dead keys removed, both catalogs).",
    "acceptance_criteria": "AC-1 clinician tab = one accordion + one Save, no flat list (tab spec); AC-2 default upsert/clear via editor (editor spec); AC-3 legacy built-in re-pick option; AC-4 stale-ref placeholder on the default select; AC-5 is_default hidden from rows, badge counts named; AC-6 collapsed summary; AC-7 admin surface unchanged (existing tests untouched); AC-8 single heading + footer gone (flat-select absence + stub-only surface asserted; the literal string-absence asserts were dropped as tautological after the keys were deleted from both catalogs); AC-9 Profile still renders the editor and its dirty check now covers is_default; AC-10 EN/FR parity; AC-11 full suite 429 + lint clean + build exit 0.",
    "lint": "npx next lint -> No ESLint warnings or errors.",
    "web_build": "npm run build -> compiled + static export OK (exit 0).",
    "ios_builds": "n/a - web-only; server contract untouched (same single PUT via updateMyProfile; resolve_context_template_key byte-identical).",
    "simplify": "4 agents (reuse/simplify/efficiency/altitude). Applied: (1) templateOptions()/bindingValue()/defaultTemplateName() collapse ~55 duplicated lines - the two selects' option lists + the summary share one source for the silent-blank-select guards (guards had already drifted: != null vs !== null); (2) sameContexts() gains is_default (altitude: Profile's Save would dead-button on a future default-only flip) + 2 tests; (3) renderSelect -> renderOrgSelect(vt) closure, shared gated on isAdmin; (4) stale comments (draftContexts rationale, 'TE-4f + a backend change' -> separate backend task, tab-spec header, test title), dead next/link mock, dead vi.fn(), tautological absence asserts, rowSelect() helper, admin-hint copy EN+FR. Conscious keeps: setDefaultTemplate self-contained over value[vt] (uniform handler pattern; delegation to updateContext had a row-position nuance); three useTranslations-hook style unchanged. Deferred with named triggers (in PR): lib-level visit-type-context model helpers -> when the VideoImportClient picker bug is fixed (chip); BUILT_IN_TEMPLATE_KEYS + MAX_CONTEXT_DESCRIPTION_LEN -> web/lib as the FIRST commit of the pin-retirement task (tab spec's stub currently fakes a divergent 3-key contract); contextsDirty JSON.stringify double-serialization (pre-existing, largest per-keystroke cost on the surface). Incident note: a regex-based test-file collapse mangled sibling describes and was reverted from HEAD before commit; the optional renderEditor() dedup was declined as not mechanically safe.",
    "notable_pre_existing": "VideoImportClient.tsx:707-713 lists is_default rows as pickable 'contexts' in the upload visit-context picker (labeled with the visit-type name, duplicating its own Use-default option) - chipped."
  }
}
