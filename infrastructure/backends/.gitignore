# Per-environment backend config files contain the AWS account ID (in the
# bucket name). They're generated by `infrastructure/bootstrap` outputs;
# regenerate locally rather than committing them.
*.tfbackend
!.gitignore
